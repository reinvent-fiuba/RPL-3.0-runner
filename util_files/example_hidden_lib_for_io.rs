// src/lib.rs

// As this is for IO tests it can be empty or nonexistent, but the teacher can decide to use it to hide aux functions.
// Then they can import it in the main via use student_package;

// use std::io;

// pub fn __aux() -> i32 {
//     let mut input = String::new();
//     io::stdin().read_line(&mut input).unwrap();
//     let n: i32 = input.trim().parse().unwrap();
//     n
// }
